import '!style-loader!css-loader!sass-loader!../src/bootstrap-5.2.3/scss/bootstrap.scss';

export const parameters = {
  actions: { argTypesRegex: "^on[A-Z].*" },
  controls: {
    matchers: {
      color: /(background|color)$/i,
      date: /Date$/,
    },
  },
}